Выполни правки по ревью в `/home/vadim/dev/mimic-web-bot`.

Источник — замечания и рекомендации из последнего ревью в этом чате. Если ревью в чате нет, напиши это одной фразой и остановись. Не коммить и не пушь.

Закрой каждое замечание правкой в коде. Рекомендацию выполняй, если она уточняет ту же правку и не меняет задачу. Если замечание или рекомендация необоснованы, код не меняй и напиши, почему вывод ревью неверен. Спорное, что без ответа нельзя отличить от намерения, не трогай — перечисли в ответе.

После правок запусти `npm test` в `/home/vadim/dev/mimic-web-bot`. Если тест падает из-за правки — исправь и запусти снова. Если из-за правки разошлись поведение и `README.md`, обнови README.

## Ответ

Кратко, по-русски:

```markdown
## Сделано
- `path` — что изменено по какому замечанию

## Не трогал
- что осталось и почему; для необоснованного замечания или рекомендации — в чём ошибка ревью

## Проверка
- `npm test`: результат
```

Пустые разделы не заполняй. Коммит после этого — отдельная команда `/commit`.
