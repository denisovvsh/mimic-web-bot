Создай коммит в git. Репозиторий: `/home/vadim/dev/mimic-web-bot`.

Сообщение коммита — на русском, 1–2 предложения, про «зачем», не про перечень файлов.

Не меняй git config. Не пушь. Не используй `--no-verify`, `--no-gpg-sign`, `commit --amend`, интерактивный `git add -i` / `git rebase -i`. Секреты (`.env`, токены, ключи) не коммить; если пользователь явно просит их включить — предупреди и не добавляй без повторного подтверждения.

Если коммитить нечего — так и напиши, пустой коммит не создавай.

## Коммит

Параллельно:

- `git status`
- `git diff` (staged и unstaged)
- `git log -5 --oneline`, чтобы попасть в стиль сообщений, но язык сообщения всё равно русский

Дальше по порядку:

1. `git add` только файлы этой правки.
2. Коммит через HEREDOC:

```bash
git commit -m "$(cat <<'EOF'
Сообщение на русском.

EOF
)"
```

3. `git status` после коммита. Если хук отклонил коммит — исправь причину и сделай новый коммит, не amend.

## После коммита

Отдельным коротким блоком напиши, что сделать, чтобы эта правка заработала у уже установленного расширения. Отдельного сервера нет: расширение загружено распакованным из этого каталога.

Пиши только шаги, которые реально нужны из-за файлов в этом коммите:

- Любая правка кода расширения: на `chrome://extensions` или `browser://extensions` нажать «Обновить» у Mimic Web Bot.
- Менялись `src/content/telemost-hook.js` или `src/content/telemost.js`: после обновления расширения перезагрузить вкладку `telemost.yandex.ru` до входа в звонок.
- Менялась боковая панель (`src/sidepanel/`): закрыть и снова открыть панель.
- Менялись только тесты, README или эта команда: в браузере ничего делать не нужно.

Не предлагай пуш, магазин Chrome и переустановку, если хватает обновления распакованного расширения.
